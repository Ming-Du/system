syntax = "proto2";
package planning;

enum PlanningMode {
  LOCAL = 0;
  GLOBAL = 1;
}

message PlanningConfig {
    optional uint32 frequency = 1;
    //optional float plate = 2;
    //optional float brand = 3;
}